// import k from "./kaboom.js";

export const GAME = {
    W: 1750,
    H: 720,
    S: 1,
}

export const PATUTI = {
    MS: 100,
    JUMP_FORCE: 500,
}

export const PATUTI_SCALE = GAME.S/3;
// export const PATUTI_DEFAULT_POSITION = (k.width() * 0.5, k.height() * 0.3);

export const AREA_SCALE = GAME.S/2;
// export const AREA_DEFAULT_POSITION = (k.width() * 0.5, k.height() * 0.7);

export const BG_SCALE = GAME.S;
